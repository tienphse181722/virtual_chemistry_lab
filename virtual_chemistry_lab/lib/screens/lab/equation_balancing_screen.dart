import 'package:flutter/material.dart';

/// SCR-008: Equation Balancing
/// Cân bằng phương trình hóa học
class EquationBalancingScreen extends StatelessWidget {
  const EquationBalancingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Cân Bằng Phương Trình')),
      body: const Center(child: Text('Equation Balancing Screen - TODO')),
    );
  }
}
