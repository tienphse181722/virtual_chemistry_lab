import 'package:flutter/material.dart';

/// SCR-007: Combustion Lab
/// Phòng lab đốt chất
class CombustionLabScreen extends StatelessWidget {
  const CombustionLabScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Đốt Chất')),
      body: const Center(child: Text('Combustion Lab Screen - TODO')),
    );
  }
}
