import 'package:flutter/material.dart';

/// SCR-006: Chemical Mixing Lab
/// Phòng lab trộn hóa chất A + B
class MixingLabScreen extends StatelessWidget {
  const MixingLabScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Trộn Hóa Chất')),
      body: const Center(child: Text('Mixing Lab Screen - TODO')),
    );
  }
}
