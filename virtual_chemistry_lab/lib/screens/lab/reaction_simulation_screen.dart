import 'package:flutter/material.dart';

/// SCR-010: Reaction Simulation
/// Mô phỏng phản ứng thời gian thực
class ReactionSimulationScreen extends StatelessWidget {
  const ReactionSimulationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Mô Phỏng Phản Ứng')),
      body: const Center(child: Text('Reaction Simulation Screen - TODO')),
    );
  }
}
