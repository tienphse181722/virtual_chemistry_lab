import 'package:flutter/material.dart';

/// SCR-009: Equipment Selection
/// Chọn thiết bị thí nghiệm
class EquipmentSelectionScreen extends StatelessWidget {
  const EquipmentSelectionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Chọn Thiết Bị')),
      body: const Center(child: Text('Equipment Selection Screen - TODO')),
    );
  }
}
